<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= pageTitle %>
    </title>
    <link rel="stylesheet" href="scss/main.css">
</head>

<body>
    <div class="banner-for-mobile">
        <h1>Sorry!!!</h1>
        <p>This site is not available for mobile/tab or devices with smaller screen(i.e 1050px)</p>
        <h3>Note:Please visit from PC/Laptop or browser on full screen mode.</h3>
    </div>
    <div class="login-form-container">
        <div class="login-form">
            <div class="login-header">
                <h1>Welcome to Hamro Blood Bank</h1>
                <div class="logo-container">
                    <img src="images/blood.png" alt="logo">
                </div>
            </div>
            <div class="login-main">
                <form action="/login" method="POST" name="login-form">
                    <input type="email" name="email" placeholder="Email" value="<%= oldValue.email %>" required>
                    <input type="password" name="password" id="password" placeholder="Password"
                        value="<%= oldValue.password %>" required>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit" class="btn btn-login btn-center">Log In</button>
                </form>
            </div>
            <div class="login-footer">
                <p>Don't have an account? <a href="/signup">Sign Up</a></p>
            </div>
        </div>
    </div>
    <% if(errMessage.length>0){ %>
        <div class="bottom-alert-container">
            <% errMessage.forEach(element=>{ %>
                <div class="alert">
                    <p class="message">
                        <%= element %>
                    </p>
                    <button class="close-btn" onclick="this.parentNode.style.display='none'">
                        X
                    </button>
                </div>

                <%})%>
        </div>
        <% } %>


</body>

</html>